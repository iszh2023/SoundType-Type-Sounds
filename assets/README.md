# Light theme background

Save the pastel image you attached in chat as:

- `Google Extension/SoundType/assets/light-bg.jpg`

Then reload the extension in `chrome://extensions` to see the Light theme.

